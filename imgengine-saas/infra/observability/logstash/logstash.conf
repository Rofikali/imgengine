input {
  file {
    path => "/data/logs/*.log"
    start_position => "end"
    sincedb_path => "/usr/share/logstash/data/sincedb"
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "imgengine-logs"
  }
}
